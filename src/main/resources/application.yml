spring:
  application:
    name: inventory-api

  datasource:
    url: ${postgresql://neondb_owner:npg_egu8Qi9sKhOS@ep-proud-unit-acjae4zp-pooler.sa-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: public

  flyway:
    enabled: false